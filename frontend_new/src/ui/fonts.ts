import { Inter, Rubik, Jacques_Francois_Shadow } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const rubik = Rubik(
    { subsets: ['latin'] }
);
export const jfs = Jacques_Francois_Shadow(
    { weight: ['400'], subsets: ['latin'] }
);